# PyDispatcher

### Command
`-ti` - time interval

`-lf` - log file name

Сommand examples:

`notepad.exe -ti 1.5 -lf newlogname.txt`

`notepad.exe -lf newlogname.txt`

`notepad.exe -ti 1.5`

`notepad.exe`
